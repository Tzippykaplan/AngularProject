import { CountdownPipePipe } from './countdown-pipe.pipe';

describe('CountdownPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CountdownPipePipe();
    expect(pipe).toBeTruthy();
  });
});
